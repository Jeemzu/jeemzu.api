{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*"
  // ConnectionStrings:DefaultConnection is set via dotnet user-secrets in Development
  // and via Azure App Service Configuration in Production.
  // Do NOT add the connection string here — it contains credentials.
}
